/**
 * @type { import("next").NextConfig}
 */
const config = {
  experimental: {
    // esmExternals: true,
    // swcLoader: true,
    // swcMinify: true,
    cpus: 4,
  },
};
module.exports = config;
